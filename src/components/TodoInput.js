import React from 'react';

const TodoInput = () => {
    return (
        <div className="TodoInput-Wrap">
            <input type="text"/>
            <button>add</button>
        </div>
    );
};

export default TodoInput;